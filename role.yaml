apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: gitlab-k8s
rules:
  - apiGroups: ["*"] 
    resources: ["*"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]

